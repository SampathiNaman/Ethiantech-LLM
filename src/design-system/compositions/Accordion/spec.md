# Accordion (Composition)

- **Composes**: Surface + Text + Icon
- **Props**: `sections` array `[{ title, content }]`, `expandedIndex`, `onChange`
- **States**: `collapsed` | `expanded`
- Controlled or uncontrolled via expandedIndex/onChange.
