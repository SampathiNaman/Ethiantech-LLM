# CommentCard (Composition)

- **Composes**: Card + Avatar + Text + Button
- **Props**: `comment` object, `onReply`, `onEdit`
- **comment shape**: `{ author, avatar, timestamp, body }`
- Pure composition. No data fetching.
