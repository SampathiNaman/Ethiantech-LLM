# CourseCard (Composition)

- **Composes**: Card + Image + Badge + Text + Rating + Button
- **Props**: `course` object, `variant`, `onEnroll`
- **course shape**: `{ title, instructor, image, badge, rating, price, originalPrice, lessons, level }`
- Pure composition. No data fetching.
