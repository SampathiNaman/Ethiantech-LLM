# Tabs (Composition)

- **Composes**: Surface + Text + Icon (optional)
- **Props**: `tabs` array `[{ label, icon, content }]`, `activeTab`, `onChange`
- **States**: `active` (brand underline) | `inactive`
- Controlled or uncontrolled (via onChange).
